# .NET build output
bin/
obj/
*.user
*.suo
*.userprefs
*.dbmdl
*.dbproj.schemaview
*.jfm
*.pfx
*.publishsettings
.vs/

# IDE
.vscode/
.idea/

# Build artifacts shipped via GitHub Releases — never commit
artifacts/
publish/
plugin.zip
manifest.sha256

# OS noise
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
*.lnk

# Logs
*.log

# Local credentials / secrets — paranoia bar
*.env
.env.local
secrets/
# .mcp.json holds a 626 agent bearer token — never commit
.mcp.json

# Community AutoHotkey reference macros — kept locally as design specs,
# third-party authored, not published into this repo
*.ahk

# Superpowers brainstorming companion (local mockups)
.superpowers/
